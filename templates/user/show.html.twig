{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <h1>Liste des utilisateurs</h1>
    <ul>
        {% for user in users %}

            <li>{{ user.username }}</li>

        {% endfor %}
    </ul>
{% endblock %}
